<form method="post" action="{{ path('fraisforfait_crud_delete', {'id': fraisforfait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fraisforfait.id) }}">
    <br/><button style="background-color: lightcoral;" class="btn">Delete</button>
</form>
