<form method="post" action="{{ path('fichefrais_crud_delete', {'id': fichefrai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fichefrai.id) }}">
    <button style="background-color: lightcoral;" class="btn">Delete</button>
</form>
